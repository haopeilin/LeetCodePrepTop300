# macOS
.DS_Store
.AppleDouble
.LSOverride

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.env
.venv/
venv/

# Node
node_modules/

# Regeneratable build artifacts
# (data.js is built by: process_data.py → split_data.py)
data.js

# Raw upstream pipeline data (regenerate via fetch_missing_content.py + download_images.py)
# Not needed by the website — browser reads from data/ instead
raw_questions/

# Temporary / intermediate pipeline files
needs_conversion.json
